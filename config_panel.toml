version = "1.0"

[main]
name = "SyncYunohost Configuration"
services = ["__APP__"]

    [main.main.syncyunohost_enabled]
    ask.en = "Would you like to enable the SyncYunohost module to synchronize Dolibarr members with YunoHost?"
    type = "boolean"
    yes = "true"
    no = "false"
    default = false
    bind = ":__INSTALL_DIR__/config.yml"

    [main.main.syncyunohost_base_domain]
    ask.en = "Choose the domain for Yunohost user's email creation:"
    type = "domain"
    bind = ":__INSTALL_DIR__/config.yml"

    [main.main.syncyunohost_main_group]
    ask.en = "Which Yunohost group do you want to sync Dolibarr's activated members with?"
    type = "group"
    default = "visitors"
    bind = ":__INSTALL_DIR__/config.yml"
[main]
